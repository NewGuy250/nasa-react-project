# APOD Project

Allows people to view the APOD (Astronomy Picture of the Day) from the APOD API from Nasa

However to view the image, user's will have to manually add their own file named ".env" and the contents to be:
VITE_NASA_API_KEY = (Enter API key here)

Which they can retrieve from https://api.nasa.gov/
